{#
/**
 * @file
 * Custom theme implementation to display a node.
 * Custom template for Talk nodes in Full view mode.
 *
 * @ingroup themeable
 */
#}

{%
  set classes = [
    'node',
    node.bundle|clean_class,
    view_mode ? view_mode|clean_class,
  ]
%}

<article{{ attributes.addClass(classes) }}>
  {{ content.scitalk_prefixed_talk_number}}
  {% block video %}
    {{ content.field_talk_video }}
    {{ content.field_talk_video_url }}
  {% endblock %}

  {{ drupal_region('page_title') }}
  {% include '@scitalk_theme/includes/speaker-fields.twig' %}

  {{ content|without('field_talk_number','scitalk_prefixed_talk_number','field_talk_video', 'field_talk_video_url', 'field_talk_speaker_profile',  'field_talk_speakers_text') }}

</article>

<aside class="related-talks-view">
  {{ drupal_view('scitalk_talks_in_collection', 'related_talks')}}
</aside>

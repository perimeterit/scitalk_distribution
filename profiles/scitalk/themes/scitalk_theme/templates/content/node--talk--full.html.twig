{#
/**
 * @file
 * Custom theme implementation to display a node.
 * Custom template for Talk nodes in Full view mode.
 *
 * @ingroup themeable
 */
#}

{%
  set classes = [
    'node',
    node.bundle|clean_class,
    view_mode ? view_mode|clean_class,
  ]
%}


{{ drupal_block('scitalk_citations_block', {context_mapping: {node: '@node.node_route_context:node'}}) }}

<article{{ attributes.addClass(classes) }}>
  <div class="video">
    {{ content.field_talk_video }}
  </div>
  <div class="content-wrapper">
    <div class="content">
      {{ content|without('links', 'field_talk_video') }}
    </div>
    <aside class="sidebar">
      <div class="related-talks-view">
        {{ related_talks_block }}
      </div>
    </aside>
  </div>
  {{ content.links }}
</article>

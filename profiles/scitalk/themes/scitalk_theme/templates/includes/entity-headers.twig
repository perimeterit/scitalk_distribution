{# Use the correct heading level depending on view mode #}
{{ title_prefix }}

{% if is_view_page %}
 {% set hl = 2 %}
{% else %}
  {% set hl = 3 %}
{% endif %}

{% if not page and (view_mode != 'page_header') %}
  <{{hl}}{{ title_attributes }}>
      <a href="{{ url }}" rel="bookmark">{{ label }}</a>
  </{{hl}}>
{% endif %}
{{ title_suffix }}
